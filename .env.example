# Postgres connection string.
# Run `docker compose up -d` first, then this value works as-is for local development.
# Note: published on host port 5433, not the Postgres default 5432.
DATABASE_URL="postgresql://authslice:localdevpassword@localhost:5433/authslice?schema=public"